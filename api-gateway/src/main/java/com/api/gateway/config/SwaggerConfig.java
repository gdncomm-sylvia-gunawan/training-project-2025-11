package com.api.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Springdoc 2.x WebFlux auto-configures:
    // - /v3/api-docs
    // - /swagger-ui/index.html
    // No custom beans needed.
}
